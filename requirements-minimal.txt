# Minimal requirements for pipeline
# Install with: pip install -r requirements-minimal.txt

# Core ML (install PyTorch first via pip install torch --index-url https://download.pytorch.org/whl/cu121)
torch>=2.1.0
torchvision>=0.16.0

# Transformers (handles all our models)
transformers>=4.40.0
accelerate>=0.27.0

# INT8 quantization
bitsandbytes>=0.42.0

# Image processing
Pillow>=10.0.0

# Data processing
numpy>=1.24.0
tqdm>=4.66.0

# Utilities
rich>=13.0.0
huggingface-hub>=0.20.0
requests>=2.31.0

# Image hashing for deduplication
imagehash>=4.3.0
