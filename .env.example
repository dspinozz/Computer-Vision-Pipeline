# Computer Vision Pipeline - Environment Variables
#
# Copy this file to .env and update values for your environment.
# For development, the defaults in docker-compose.yml work out of the box.
# For production, change all passwords and secrets.

# PostgreSQL
POSTGRES_USER=cvpipeline
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=cvpipeline_images

# Qdrant
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=image_embeddings

# API
API_HOST=0.0.0.0
API_PORT=8000

# Paths (optional, defaults work for Docker)
MODELS_DIR=/data/models
OUTPUT_DIR=/data/outputs
DATA_DIR=/data/images
